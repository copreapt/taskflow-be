services:
  backend:
    image: taskflow-backend:production
    ports:
      - "3001:3001"
    environment:
      PORT: "3001"
      NODE_ENV: "production"
      MONGODB_URI: ${MONGODB_URI}
    restart: unless-stopped
